{
  "name": "Drop",
  "version": "1.0.0",
  "description": "drop",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass --watch scss -o css --output-style compressed",
    "dev": "lite-server start"
  },
  "author": "DROP MOBILITY SRL",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "node-sass": "^5.0.0"
  }
}
